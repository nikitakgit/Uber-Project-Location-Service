
spring:
  application:
    name: UberProject-LocationService
  data:
    redis:
      host: localhost
      port: 6380

server:
  port: 7777

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
